# Android / Gradle / CMake build outputs
build/
.gradle/
.cxx/
.externalNativeBuild/
local.properties
captures/
*.apk
*.aab

# IDEs
.idea/
*.iml
.vscode/

# Python
tools/py/.venv/
__pycache__/
*.pyc

# Benchmark data: large, kept locally (docs/PLAN.md M3). Full-size screenshots and crops stay
# local too; the half-size <app>.png copies are committed. bench/out/ is harness output, full-size
# contact sheets and clips (tools/py/bench.py).
bench/**/*.raw
bench/**/*.mp4
bench/**/*_full.png
bench/**/*_crop.png
bench/out/

# macOS
.DS_Store

# clangd compile database (generate from build/core)
/compile_commands.json
